import ProjectOverview from "@/components/ProjectOverview";

export default function App() {
    return (
        <div className={"flex flex-row size-screen items-center justify-center bg-background"}>
            <ProjectOverview/>
        </div>
    )
}